info:
  name: UpdateGrade (negative)
  type: graphql
  seq: 6

graphql:
  method: POST
  url: http://localhost:5052/graphql
  body:
    query: |-
      mutation {
        updateGrade(id: 999, grade: 10) {
          id
          name
          grade
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
