info:
  name: UpdateStudent
  type: graphql
  seq: 4

graphql:
  method: POST
  url: http://localhost:5052/graphql
  body:
    query: |-
      mutation {
        updateGrade(id: 1, grade: 10) {
          id
          name
          grade
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
