info:
  name: GetStudentById (negative)
  type: graphql
  seq: 7

graphql:
  method: POST
  url: http://localhost:5052/graphql
  body:
    query: |-
      query {
        studentById(id: 999) {
          id
          name
          grade
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
